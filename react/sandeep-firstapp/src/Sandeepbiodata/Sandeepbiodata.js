import React from 'react';
const sandy=(props) => {
return (
    <div>
    <p>My Age is:{props.Age} </p>
    <p> Address:{props.Address}</p>
    <p>Credit Score: {props.CreditScore}</p>
    <p> Billing_Address:{props.Billing_Address}</p>

    </div>

) };
export default sandy;